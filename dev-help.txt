$user = new \App\WRLA\User;
// $user = \App\WRLA\User::where("email", "=", "craig.dennis@webregulate.co.uk")->first();

$user->fill([
    "name"              => "Craig Dennis",
    "email"             => "craig.dennis@webregulate.co.uk",
    "email_verified_at" => \Carbon\Carbon::now()->toDateTimeString(),
    "password"          => Hash::make("USER PASSWORD"),
    "permissions"       => json_encode([
                            "master" => true,
                            "admin" => true
                        ]),
    "settings"          => json_encode([
                            "email_notifications" => [
                                "keys_here" => true
                            ]
                        ]),
    "data"              => json_encode([
                            "data_here" => true
                        ])
]);

$user->save();